<?xml version="1.0" ?>
<!-- Minimal Detekt baseline.
     Keep this file empty by default so CI enforces fixes rather than silencing issues.

     To regenerate a baseline with current project issues run locally:
       ./gradlew detektBaseline

     If you need to preserve specific existing issues, run the task, review the
     generated file, then commit it to `config/detekt/baseline.xml`.
-->
<SmellBaseline>
  <ManuallySuppressedIssues />
  <CurrentIssues />
</SmellBaseline>
