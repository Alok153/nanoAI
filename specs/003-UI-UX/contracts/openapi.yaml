```yaml
openapi: 3.0.3
info:
  title: nanoAI UI/UX Contracts (placeholder)
  version: '0.1.0'
paths:
  /user/profile:
    get:
      summary: Get UI metadata for user
      responses:
        '200':
          description: UserProfile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserProfile'
components:
  schemas:
    UserProfile:
      type: object
      properties:
        id:
          type: string
        displayName:
          type: string
        themePreference:
          type: string
      required: [id, displayName]

```
