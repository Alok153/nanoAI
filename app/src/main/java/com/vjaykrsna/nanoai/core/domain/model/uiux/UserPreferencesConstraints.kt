package com.vjaykrsna.nanoai.core.domain.model.uiux

/** Centralises UI preference limits to prevent domain ↔ data coupling. */
object UserPreferencesConstraints {
  const val MAX_RECENT_COMMANDS = 12
}
