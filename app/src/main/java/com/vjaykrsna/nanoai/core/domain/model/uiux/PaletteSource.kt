package com.vjaykrsna.nanoai.core.domain.model.uiux

/** Entry points that can trigger the command palette overlay. */
enum class PaletteSource {
  KEYBOARD_SHORTCUT,
  TOP_APP_BAR,
  QUICK_ACTION,
  UNKNOWN,
}
